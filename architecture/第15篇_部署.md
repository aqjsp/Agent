# 第 15 篇：Agent 部署不是加个 Docker

---

无状态 API 的扩缩是加副本、前面挂负载均衡。Agent 服务默认不是无状态：

- Checkpoint 绑定 `thread_id`
- HITL 可能挂几分钟到几天
- 同一会话的下一跳要能找到上一份 State
- 进程死了要能从第 4 篇的快照醒过来

把三个副本丢到 K8s Deployment 里，会话打到另一台，要么丢上下文，要么两台同时写同一 `thread_id`。第 10 篇禁掉的全局变量，会以「本地内存 checkpointer」的形式回来。

---

### 一、先定状态放哪

**内存 checkpointer：** 只配单进程开发。进程一重启，HITL 全死。

**外置 checkpointer：** Postgres / Redis。所有副本读同一份。这是生产底线。LangGraph 的 `PostgresSaver` 一类就是干这个。

**工作流引擎：** 等待超过秒级、跨服务补偿，状态给 Temporal（第 12 篇）。图只负责一次唤醒内的推理。

会话亲和（sticky session）只能当优化，不能当正确性。机器要下线。正确性靠外置状态。

---

### 二、副本之间争同一 thread

两个请求带同一个 `thread_id` 打到两个副本：一个在跑节点，一个在 resume HITL。没有锁就会分叉。

最低限度：按 `thread_id` 分布式锁，跑完释放。锁超时要跟节点超时对齐，避免死锁（第 6 篇隔离）。更干净：同一 `thread_id` 的命令进单分区队列，单消费者。

---

### 三、HITL 等待怎么部署

人审期间 **不要占着工作线程。** `interrupt` 后进程应退出这次 run，HTTP 返回 `202 + resume_token`。人点了再 POST 回来。线程池按「正在推理的图」扩，不按「等老板点头的单」扩。这是第 2 篇和第 4 篇在运维上的交点。

---

### 四、发布

模型版本、工具版本、图版本要能一起钉在 Checkpoint 的 metadata 里。只更模型、旧会话 resume 进新图，边对不上。灰度按 `thread_id` 哈希，不要按用户随机——同一用户连续两跳不能跨两个图版本。

健康检查：进程活着不够，还要 checkpointer 可写、模型 API 可达、熔断器没全开。后两项失败应摘流量，而不是继续 200。

---

### 收尾

有状态服务的部署单元是「图版本 + checkpointer + 按 thread 的串行化」，不是容器镜像。镜像只是包装。

下一篇把工具执行从「宿主机上的 subprocess」挪进沙箱：进程、容器、VM，各自挡住什么、挡不住什么。

我是Q，下篇见。
